[metadata]
name = licensync
version = 0.1.0
description = License compatibility checker with jurisdiction support
author = Teresa Huang
license = MIT

[options]
package_dir =
    = .
packages = find:

[options.packages.find]       # <-- explicit include / exclude list
include =
    licensync*
exclude =
    react*
